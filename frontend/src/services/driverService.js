export const getDrivers = async (params) => {
  console.log('driverService.getDrivers placeholder', params);
  return [];
};

export const getDriver = async (id) => {
  console.log('driverService.getDriver placeholder', id);
  return null;
};

export const createDriver = async (data) => {
  console.log('driverService.createDriver placeholder', data);
  return data;
};

export const updateDriver = async (id, data) => {
  console.log('driverService.updateDriver placeholder', id, data);
  return data;
};
